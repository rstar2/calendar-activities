# Steps - Interested only on the Android development/building/etc...

- create a simple Expo app `npx create-expo-app native`
- Running `npm run android` will use ExpoGo and run 
- configure EAS for building 